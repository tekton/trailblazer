version: 0.2
phases:
  install:
    runtime-versions:
      python: 3.7
  build:
    commands:
      - mkdir python
      - pip install -r requirements.txt --target ./python
      - ls -lah
artifacts:
  files:
    - "**/*"
  name: $(date +%Y-%m-%d)
